jobs:
  include:
    - os: linux
      dist: bionic

language: python
python:
    - 3.6
    - 3.7
    - 3.8
    #- pypy3.6-7.1.1

branches:
  only:
    - master

before_install:
  - sudo apt-get update -qq
  - export CODECOV_TOKEN="f9aadfe0-4412-42d2-9f39-28025e4f63a7"

cache: pip

install:
  - pip install -r requirements.txt

before_script:
  - python setup.py install

script:
  - tox

after_success:
  - bash <(curl -s https://codecov.io/bash)


notifications:
  email:
    recipients:
      - giuseppedilena92@gmail.com
    on_success: never
    on_failure: never